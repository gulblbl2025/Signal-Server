version: 2.1

executors:
  win:
    machine:
      enabled: true
      image: windows-server-2022-gui:current

jobs:
  build-windows:
    executor: win
    steps:
      - checkout
      - run:
          name: Windows 命令演示
          command: |
            echo Hello from Windows!
            systeminfo
            whoami

workflows:
  version: 2
  test-win:
    jobs:
      - build-windows
